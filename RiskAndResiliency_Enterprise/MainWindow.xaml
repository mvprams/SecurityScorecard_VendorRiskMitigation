<!-- MainWindow.xaml -->
<Window x:Class="SecurityScoreCard_VendorRiskMitigation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TrueReal.io - Vendor Risk Mitigation Strategy Generator" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="SubHeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="250"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="20" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.2" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock Text="TrueReal.io Vendor Risk Assessment" Style="{StaticResource HeaderText}"/>
                    <TextBlock Text="Secure Digital Real Estate Transaction Platform" Foreground="#666" FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#FF5252" CornerRadius="5" Padding="10,5" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="High Risk: " Foreground="White" FontWeight="Bold"/>
                            <TextBlock x:Name="HighRiskCount" Text="0" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FFC107" CornerRadius="5" Padding="10,5" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Medium Risk: " Foreground="White" FontWeight="Bold"/>
                            <TextBlock x:Name="MediumRiskCount" Text="0" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#4CAF50" CornerRadius="5" Padding="10,5" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Low Risk: " Foreground="White" FontWeight="Bold"/>
                            <TextBlock x:Name="LowRiskCount" Text="0" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Filter Controls -->
        <Border Grid.Row="1" Background="White" Padding="15" Margin="10,0,10,10" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Filter by Relationship:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <ComboBox Grid.Column="1" x:Name="RelationshipFilter" Margin="0,0,20,0" Height="30">
                    <ComboBoxItem Content="All Relationships" IsSelected="True"/>
                </ComboBox>

                <TextBlock Grid.Column="2" Text="Risk Level:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <ComboBox Grid.Column="3" x:Name="RiskFilter" Margin="0,0,20,0" Height="30">
                    <ComboBoxItem Content="All Risk Levels" IsSelected="True"/>
                    <ComboBoxItem Content="High Risk (D, F, ?)"/>
                    <ComboBoxItem Content="Medium Risk (B, C)"/>
                    <ComboBoxItem Content="Low Risk (A)"/>
                </ComboBox>

                <Button Grid.Column="4" Content="🔍 Apply Filters" Style="{StaticResource ModernButton}" Click="ApplyFilters_Click"/>
                <Button Grid.Column="5" Content="📊 Load CSV Data" Style="{StaticResource ModernButton}" Click="LoadData_Click" Background="#4CAF50"/>

                <Button Grid.Column="7" Content="🤖 Generate Risk Strategy" Style="{StaticResource ModernButton}" 
                        Click="GenerateStrategy_Click" Background="#FF6B35" MinWidth="180"/>
            </Grid>
        </Border>

        <!-- Vendor Data Grid -->
        <Border Grid.Row="2" Background="White" Margin="10,0,10,10" CornerRadius="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Vendor Security Assessment" Style="{StaticResource SubHeaderText}"/>

                <DataGrid Grid.Row="1" x:Name="VendorDataGrid" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          SelectionMode="Extended"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Background="White"
                          BorderThickness="0"
                          RowHeight="35">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F0F0F0"/>
                            <Setter Property="Foreground" Value="#333333"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Select" Width="50" Binding="{Binding IsSelected}"/>
                        <DataGridTextColumn Header="Company" Width="200" Binding="{Binding Company}"/>
                        <DataGridTextColumn Header="Relationship" Width="120" Binding="{Binding Relationship}"/>
                        <DataGridTextColumn Header="Domain" Width="150" Binding="{Binding Domain}"/>
                        <DataGridTemplateColumn Header="Grade" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="3" Padding="5,2" HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Grade}" Value="A">
                                                        <Setter Property="Background" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Grade}" Value="B">
                                                        <Setter Property="Background" Value="#8BC34A"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Grade}" Value="C">
                                                        <Setter Property="Background" Value="#FFC107"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Grade}" Value="D">
                                                        <Setter Property="Background" Value="#FF9800"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Grade}" Value="F">
                                                        <Setter Property="Background" Value="#F44336"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Grade}" Value="?">
                                                        <Setter Property="Background" Value="#9E9E9E"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Grade}" Foreground="White" FontWeight="Bold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="%" Width="50" Binding="{Binding Percentile}"/>
                        <DataGridTextColumn Header="App Sec" Width="65" Binding="{Binding ApplicationSecurity}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ApplicationSecurity}" Value="F">
                                            <Setter Property="Foreground" Value="Red"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ApplicationSecurity}" Value="D">
                                            <Setter Property="Foreground" Value="OrangeRed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Network" Width="65" Binding="{Binding NetworkSecurity}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding NetworkSecurity}" Value="F">
                                            <Setter Property="Foreground" Value="Red"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding NetworkSecurity}" Value="D">
                                            <Setter Property="Foreground" Value="OrangeRed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Info Leak" Width="65" Binding="{Binding InformationLeak}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding InformationLeak}" Value="F">
                                            <Setter Property="Foreground" Value="Red"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding InformationLeak}" Value="D">
                                            <Setter Property="Foreground" Value="OrangeRed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Endpoint" Width="65" Binding="{Binding EndpointSecurity}"/>
                        <DataGridTextColumn Header="Patching" Width="65" Binding="{Binding PatchingCadence}"/>
                        <DataGridTextColumn Header="Social Eng" Width="70" Binding="{Binding SocialEngineering}"/>
                        <DataGridTextColumn Header="Risk Level" Width="80" Binding="{Binding RiskLevel}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RiskLevel}" Value="High">
                                            <Setter Property="Foreground" Value="Red"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding RiskLevel}" Value="Medium">
                                            <Setter Property="Foreground" Value="Orange"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding RiskLevel}" Value="Low">
                                            <Setter Property="Foreground" Value="Green"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Background="#2196F3" Foreground="White" Height="35">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Status: " FontWeight="Bold"/>
                    <TextBlock x:Name="StatusText" Text="Ready"/>
                </StackPanel>
            </StatusBarItem>
            <Separator Background="White" Opacity="0.3"/>
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Selected Vendors: " FontWeight="Bold"/>
                    <TextBlock x:Name="SelectedCount" Text="0"/>
                </StackPanel>
            </StatusBarItem>
            <Separator Background="White" Opacity="0.3"/>
            <StatusBarItem>
                <ProgressBar x:Name="LoadingProgress" Width="150" Height="20" Visibility="Collapsed" IsIndeterminate="True"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Risk Mitigation Strategy Output -->
        <Border Grid.Row="4" Background="White" Margin="10" CornerRadius="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="AI-Generated Risk Mitigation Strategy" Style="{StaticResource SubHeaderText}"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <RichTextBox x:Name="StrategyOutput" 
                                 Background="#FAFAFA" 
                                 BorderThickness="1" 
                                 BorderBrush="#E0E0E0"
                                 Padding="10"
                                 IsReadOnly="True"
                                 FontFamily="Segoe UI"
                                 FontSize="14"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>